# Bio Pipeline KG Matcher —— 面向自然语言的生信流程路由与数据匹配系统

> **2026-07-22 架构更新**：当前运行时工具唯一真源已切换为 Neo4j，现有 24 个 tool（12 atomic、11 pipeline、1 task pipeline）和 14 条经审核 NEXT。预制菜展示 Neo4j pipeline 及其 `HAS_STEP` recipe；自助餐只能使用 Neo4j atomic tool；“能做什么/有哪些流程或工具”使用非执行型 `capability` 能力目录模式。旧 WDL 只作历史审查文件，不再作为 API、MCP、提示词或展示的工具来源。本文其余早期架构与 benchmark 记录仅供历史参考，当前契约以 `README.md` 为准。

> 组会汇报稿 · 2026-07

---

## 1. 项目定位与动机

实验室已沉淀两类核心资产，但二者之间缺少有效的连接层：

- **流程资产**：14 个标准化 WDL 分析流程，覆盖上游数据处理、表达分析、突变分析、生存分析四大类；
- **数据资产**：以知识图谱（KG）组织的研究队列 / 样本 / 测序文件元数据，规模达成百上千条记录。

现实痛点在于**流程与数据的匹配高度依赖专家经验**：使用者需同时掌握"哪个流程叫什么、需要什么输入、对应仓库中的哪批数据"三重知识。这一门槛使新成员、外部合作者以及临时性分析需求，难以自助完成从"想法"到"可执行方案"的转化。

本项目的重点已从早期"根据用户已有文件格式推荐工具链"，调整为**面向自然语言问题的 pipeline 路由**：

1. 用户可以不上传文件，只输入一句自然语言问题；
2. 系统从 Neo4j 的 pipeline/task-pipeline 闭集中选择合适流程；
3. 系统展示所选 pipeline 的 Neo4j `HAS_STEP` 锁定步骤，未拆解的流程作为 pipeline-level tool 展示；
4. 系统依据问题中的癌种、组学类型、格式、分析目标，在 KG 数据源中检索队列级与文件级数据；
5. 系统输出结构化 JSON + 自然语言摘要，便于 Web Demo、MCP、Agent 或后续执行系统接入。

> **边界说明**：当前系统不直接执行 WDL/Cromwell，也不真正运行生信分析。当前阶段的目标是"选对流程、解释流程、找到数据、形成可执行前的结构化计划"。

---

## 2. 系统架构总览

系统采用四级串联流水线，对外通过 MCP（Model Context Protocol）协议暴露服务：

```
自然语言需求
     │
     ▼
[① 意图理解]   槽位抽取：analysis_goal / disease / omics_type / input_hint / quant_hint
     │        （规则抽槽位 + LLM 语义补充，LLM 为主、规则保底）
     ▼
[② 流程匹配]   LLM 闭集多选 + 自评置信度（规则打分/精排作召回兜底）
     ▼
[③ 工具契约]   从 Neo4j 加载 pipeline/tool/slot/NEXT/HAS_STEP
     ▼
[④ 数据匹配]   基于数据画像约束，从 KG CSV 检索队列 / 文件 / 数据组合
     ▼
结构化结果（JSON）+ 中文渲染摘要
```

各层均为**无异常穿透设计**：任一环节失败即优雅降级，保证对外始终返回可读结果。

---

## 3. LLM 参与意图理解详解（核心）

> 这是本系统的主要办法。规则路由是**可复现的保底基线**，LLM 才是应对真实、开放、非标准化表达的主力手段。

### 3.1 LLM 的定位：语义理解与闭集选择，而非执行器

在本系统中，LLM **不是执行器**。它明确不做以下事情：

- 不执行 pipeline，不调用 Cromwell，不生成任意 Shell 命令；
- 不直接查询任意数据库，不自由编造工具名；
- 不输出不可控的自然语言计划作为唯一结果。

LLM 的职责被严格限定在**自然语言理解 + 闭集选择辅助**：

1. 理解用户需求中的分析目标；
2. 识别癌种、组学类型、输入格式、期望输出；
3. 在 14 个 pipeline 闭集内做选择；
4. 在规则召回不确定时提供语义补充；
5. 输出**固定 JSON 字段**，交给确定性程序校验。

### 3.2 为什么必须用 LLM

用户的表达通常不标准化。典型例子：

> "我想看看肝癌样本能不能根据表达谱自动分成几个亚型。"

这句话既没说 `rnaseq_unsupervised_cluster`，也没说 `count matrix`，但它隐含表达了：

- 组学类型：bulk RNA-seq / 表达谱；
- 分析目标：无监督聚类 / 亚型识别；
- 期望结果：样本分群。

规则系统能覆盖常见关键词，但面对"表达亚型""自动分成几类""潜在分群"这类多样表达，需要 LLM 把它们归并到同一分析目标。**这正是"LLM 为主、规则保底"的原因**——规则保证下限，LLM 抬高上限。

### 3.3 LLM 在两个层次介入，输出字段不同

系统在**两个不同层次**调用 LLM，两者的输出契约（schema）不同，需分开理解：

#### 层次 A —— pipeline 选择层（当前生产主路径）

位于 `pipeline_router.py` 的 `_merge_llm_selection`。在规则打分完成后，把带有数据画像标注的完整菜单交给 LLM，让它做闭集多选**并给出每个流程的置信度**。

**LLM 输出字段（严格 JSON）：**

```json
{
  "selections": [
    {"pipeline_id": "diff_expr_kegg", "confidence": 0.95, "reason": "用户提到 KEGG 通路富集，与该流程完全对应。"},
    {"pipeline_id": "diff_expr_go", "confidence": 0.9, "reason": "用户同时提到 GO 功能富集。"}
  ]
}
```

| 字段 | 类型 | 含义 |
|------|------|------|
| `selections` | array[object] | 从 14 个 pipeline ID 闭集中选出的结果，**允许多选**（最多 5 个），按 `confidence` 从高到低排序 |
| `selections[*].pipeline_id` | string | 命中的流程 ID，必须落在 14 个闭集内 |
| `selections[*].confidence` | number | **LLM 自己给的契合度打分（0~1）**，直接作为最终置信度（见 4.2 与 3.5） |
| `selections[*].reason` | string | LLM 对该流程的简短说明，会写入推荐理由 |

> **兼容性**：仍兼容旧的 `{"pipeline_ids": [...], "reason": "..."}` 格式——若模型只返回 `pipeline_ids`，命中项回退到规则分/0.82 地板。

#### 层次 B —— 细粒度意图槽位层（`intent.py`，更细的语义解析）

位于 `intent.py` 的 `extract_intent` + `_call_llm`。它把自由文本解析为更细的意图槽位，同样要求严格 JSON。

**LLM 输出字段（严格 JSON）：**

```json
{
  "input_format": "maf",
  "target_function": "肿瘤突变负荷生存分析流程",
  "cohort": "食管癌",
  "ambiguous": false
}
```

| 字段 | 类型 | 含义 |
|------|------|------|
| `input_format` | string \| null | 归一化到既定格式词表的输入格式（如 `maf`/`fq.gz`/`tsv`） |
| `target_function` | string \| null | **必须是能力菜单里某个 function 的原文**；无合适项则为 `null` 且 `ambiguous=true` |
| `cohort` | string \| null | 抽取的癌种字符串，无则 `null` |
| `ambiguous` | boolean | 是否存在明显歧义 |

> 两层的共同点：**LLM 只输出结构化字段，绝不输出自由文本作为最终结果**；所有字段都要过确定性校验。

> **生产路径说明**：`route()` 的意图提取现已默认走 LLM（`PipelineRouter._llm_intent`，与层次 B 同源、闭集约束一致），输出 `disease / omics_type / input_format / quant_hint / analysis_goal / requested_outputs / ambiguous`，逐字段过词表校验；LLM 留空的槽位由规则值补齐，`intent.source` 标记为 `llm`（`FORCE_RULE=1` 或 LLM 不可用时降级为 `rule`）。这样癌种、格式等线索靠语义理解而非关键词命中，数据匹配更准。

### 3.4 提示词（Prompt）设计——闭集约束是灵魂

以层次 A 的 pipeline 路由为例，system prompt 的核心结构：

```text
你是生信 pipeline 路由器。
只能从菜单中的 pipeline_id 多选，禁止自造 id。
输出严格 JSON: {"selections":[{"pipeline_id":"...","confidence":0.0~1.0,"reason":"..."}]}。
最多返回 5 个，按 confidence 从高到低排序。
【confidence 打分规则】confidence 表示"这个流程有多契合用户需求"：
  · 1.0        = 需求与该流程完全对应、无歧义，且只有它合适；
  · 0.85~0.95  = 明确契合，是最合理选择，仅存极小不确定；
  · 0.6~0.8    = 相关可用，但表述有歧义、或有其它流程也说得通；
  · 0.4~0.6    = 勉强相关的备选（低于 0.4 不返回）。
  只有一个流程明确对应时，大胆给到 0.95~1.0，不要保守压低。
选择时必须同时考虑分析目标和 data_roles / data_rule：
  · FASTQ 上游流程不要选表达矩阵流程；
  · 表达矩阵下游分析不要选 FASTQ 上游流程；
  · MAF/突变下游分析不要选 RNA-seq 表达流程。
【GO 富集 vs 通路富集 的严格区分】
  · diff_expr_go  = 差异表达 + GO 功能富集：仅当提到 GO / 功能富集 / 生物学过程(BP)/分子功能(MF)/细胞组分(CC) 时选。
  · diff_expr_kegg = 差异表达 + 通路富集：凡提到 KEGG / Reactome / 通路 / 信号通路 / pathway / 基因落在哪些通路，一律选它。
  · 只提通路→只返 kegg；只提 GO→只返 go；两者都提→都返回。

能力菜单：
- cellranger_workflow: ... | keywords=... | data_roles=[...] | data_rule=...
- rnaseq_singletask:   ... | keywords=... | data_roles=[...] | data_rule=...
  ...（14 条）

用户需求：<原文>
已解析 intent：<规则抽出的 intent JSON>
```

Prompt 设计的四个关键点：

- **闭集约束**：模型只能从 14 个 ID 里选，从机制上杜绝幻觉工具名、不存在的 pipeline、不可执行的分析路径；
- **LLM 自评置信度**：模型对每个命中流程按 0~1 打分并附标尺，需求明确对应时给 0.95~1.0，有歧义时下调——置信度从"规则算术"转为"语义判断为主"（见 3.5、4.2）；
- **允许多流程**：如"同时做 GO 和 KEGG 富集"天然对应两个流程，允许返回两条 `selections`；
- **易混流程定向消歧**：针对"差异表达 + 富集"这一高频混淆点，明确规定"通路 / pathway / KEGG / Reactome"归 `diff_expr_kegg`、"GO / 功能富集"归 `diff_expr_go`、仅提其一不附带另一个——此规则直接把 96 例中 7 道通路富集误判修正为满分（详见第 9 章）；
- **携带数据画像**：菜单里同时给出每个流程的 `data_roles`/`data_rule`，让 LLM 在选流程时就避免"上游/下游数据错配"。

调用侧的技术细节（`_call_llm`）：走 OpenAI 兼容接口，`temperature=0` 保证稳定，请求 `response_format={"type":"json_object"}` 强制 JSON；若接口不支持则自动去掉该参数重试；返回内容用 `_extract_json_object` 剥离 ``` 代码围栏并解析第一个 JSON 对象——对 LLM 输出格式不规范做了充分容错。

### 3.5 校验与融合——LLM 建议，程序拍板

LLM 的输出绝不直接采信，而是经过确定性校验后**与规则结果融合**：

1. **合法性校验**：`selections[*].pipeline_id`（或旧格式 `pipeline_ids`）必须落在 14 个已知 ID 中；非法 ID 直接丢弃；`target_function` 必须命中能力菜单原文，否则尝试子串匹配、再不行置 `null`；
2. **降级兜底**：若 LLM 不可用、超时、或返回无合法结果，系统退回规则路由（意图标记 `degraded=true`）；
3. **融合重排**：**LLM 给出的 `confidence` 直接作为该流程的最终置信度**（LLM 判断为主，不再被规则分覆盖、也不受 0.99 上限约束，明确匹配可达 1.0）；仅当 LLM 未给分（旧格式 `pipeline_ids`）时才回退规则分或 0.82 地板。规则命中但 LLM 未选的候选补在后面，共同参与最终 Top-K 排序；
4. **可追溯**：LLM 的用量元数据（模型名、prompt/completion/total tokens、缓存命中）全程记录在 `llm_metadata`，最终响应结构始终由确定性程序拼装，**LLM 不控制整体输出结构**。

一句话概括这套机制：

```text
规则召回与打分  +  LLM 闭集语义补充  +  程序校验  +  最终排序
```

### 3.6 当前默认策略：Demo 稳定优先

为组会 Demo 的稳定性，当前默认启用规则路由（`FORCE_RULE=1`）。原因是：

- 规则路由不依赖外部 API，输出稳定、可复现，benchmark 分数可控；
- 避免现场因 LLM 网络 / API 抖动导致结果不稳定。

**LLM 闭集增强能力始终保留**——将 `FORCE_RULE=0` 并配置好凭证，系统即调用 LLM 做语义补充。也就是说：**设计定位上 LLM 是主力，规则是保底；演示时为求稳先走保底，一键即可切回主力。**

---

## 4. 规则保底路由（`extract_intent` + `match_pipelines`）

规则路径既是 LLM 关闭时的完整替代，也是 LLM 开启时的召回底座。

### 4.1 槽位抽取

基于关键词命中表把文本解析为闭集槽位：

| 槽位 | 来源命中表 | 示例 |
|------|-----------|------|
| `analysis_goal` | `ANALYSIS_HINTS` | GO 富集、无监督聚类、TMB 生存分析 |
| `disease` | `DISEASE_ALIASES` | 胶质瘤、肝癌、黑色素瘤（含中英文与缩写别名） |
| `omics_type` | `OMICS_HINTS` | bulk RNA-seq、WES/MAF、scRNA-seq |
| `input_hint` | `FORMAT_HINTS` | fq.gz、maf、tsv、xls、xlsx |
| `quant_hint` | 关键词 | FPKM、TPM、counts |

### 4.2 流程打分

**生产路径下置信度以 LLM 自评为主**：LLM 对每个命中流程按 0~1 打分（见 3.3 层次 A、3.5），直接作为最终置信度——需求明确对应可达 **1.0**，有歧义时下调。以下规则打分仅在 **LLM 关闭/不可用**（`FORCE_RULE=1` 或降级）、或 LLM 只返回旧格式 `pipeline_ids` 未给分时，作为**兜底与召回排序**使用。

规则打分对 14 个流程逐一打分，核心维度与权重：

| 维度 | 权重逻辑 |
|------|---------|
| 规则术语命中（`PIPELINE_RULES.terms`） | 每命中一项 +10 |
| 知识卡关键词命中 | 每命中一项 +3 |
| 组学类型一致 | +4 |
| 输入格式一致 | +2 |
| 流程全名出现在原文 | +12 |
| **精排加减分**（`_precision_boost`） | 见 4.3 |

规则置信度归一化为 `min(0.99, 0.35 + score/40)`（**规则路径封顶 0.99**，永不到 1.0），取 Top-K。规则完全无命中时，基于组学类型做保守兜底。

### 4.3 精排消歧（`_precision_boost`）——准确率的关键

生信领域存在大量"表述相似但意图不同"的陷阱，通过定向加减分消歧：

| 触发语义 | 处理 |
|---------|------|
| `unmapped BAM` / `uBAM` / `read group` | → `paired_fastq_to_unmapped_bam` |
| `Mutect2` / `somatic VCF` / `BQSR` / `SnpEff` / 配对 WES | → `wes_somatic_pair` |
| `FastQC` / `Trim` / `STAR` / `RSEM` / `FeatureCounts` / `MultiQC` | → `rnaseq_singletask` |
| 共表达 / 模块 / hub gene / 临床性状 / 肿瘤分级 | → `wgcna` |
| 性别 + 驱动基因 + 突变频率 | → `driver_gene_gender_analysis` |
| 亚型 / 分子分型 / 自动分群 | → 无监督聚类，并对富集分析**扣分** |

典型消歧：「性别 + 共表达/模块」判为 WGCNA（性别作性状），而非驱动基因分析；「性别 + 驱动基因 + 突变频率」才判为驱动基因性别分层——两者靠加减分精确分开。

---

## 5. Neo4j 工具契约层

`Neo4jClient.tool_catalog()` 一次加载 pipeline tool、atomic tool、输入输出 slot、
artifact/format、审核后的 `NEXT` 和 pipeline `HAS_STEP`。预制菜若存在锁定
`HAS_STEP` 就按 Neo4j recipe 展开，否则显示为单个 pipeline-level tool。
自助餐只向 LLM 提供 `tool_kind=atomic` 的闭集，并在模型返回后再次校验
tool ID、slot 名、前序引用和 `NEXT`。运行模块中不存在 WDL 工具解析器。

---

## 6. 数据匹配层（`CsvKGDataMatcher`）

从 KG CSV 为推荐流程检索可用数据。**核心创新是「数据画像」（Data Profile）约束**：每个流程绑定一份画像（`PIPELINE_DATA_PROFILE_KEYS` → `DATA_PROFILE_TEMPLATES`），规定其合法输入角色（fastq / expression / clinical / metainfo / maf 等）。

- **角色过滤**：上游只匹配 FASTQ，表达下游只匹配表达矩阵，突变下游只匹配 MAF——杜绝"下游分析误配上游原始数据"；
- **智能配对**：自动识别并配对 FASTQ 的 R1/R2；生存分析类自动配齐「MAF + 临床 + 元信息」三件套；
- **数据分级偏好**：优先推荐处理后的 T2 数据，避免让用户从原始 FASTQ 重跑；
- **数据集精排**（`_preferred_study_bonus`）：针对特定流程 + 特定研究的已知最佳组合定向加分。

**两个数据来源**：KG CSV 的 T1 原始文件和 T2 分析文件。运行时不再读取历史 pipeline 目录中的 `example_inputs.json`，避免示例路径被当成真实可用数据。

输出分三级：`cohort_candidates`（队列）、`file_candidates`（文件）、`data_combinations`（可直接使用的数据包，kind 为 `paired_fastq` / `expression_bundle` / `mutation_bundle`）。

---

## 7. 系统输出结构

`route_pipeline_request` / `/api/ask` 返回的顶层结构：

```json
{
  "nl": "配对 WES 数据完成体细胞变异检测",
  "intent": { "analysis_goal": "体细胞变异检测", "omics_type": "WES/MAF", "source": "llm", "ambiguous": false },
  "capabilities_count": 12,
  "result": { "...完整结构化结果..." },
  "matched_pipelines": [
    {
      "pipeline_id": "wes_somatic_pair",
      "name": "体细胞 WES 配对样本变异检测流程",
      "reason": "命中 配对 wes, 体细胞变异, WES...",
      "confidence": 0.99,
      "matched_terms": ["配对 wes", "体细胞变异", "WES"],
      "internal_steps": [ "...Neo4j tool / HAS_STEP recipe..." ],
      "inputs": [ "..." ], "outputs": [ "..." ]
    }
  ],
  "matched_data": { "cohort_candidates": [], "file_candidates": [], "data_combinations": [], "query_constraints": {} },
  "agent_input": {
    "pipeline_id": "tmb_survival_analysis",
    "files": [
      "/hpcdisk1/cbb_group/data/analysis/HRA000071/HRA000071-SomaticSNV-1.0.maf",
      "/hpcdisk1/cbb_group/data/analysis/HRA000071/HRA000071-Clinical-1.0.xlsx",
      "/hpcdisk1/cbb_group/data/analysis/HRA000071/HRA000071-MetaInfo-1.0.xlsx"
    ],
    "files_text": "…每行一个绝对路径…",
    "feasibility": { "ok": true, "required_roles": ["maf","clinical","metainfo"], "present_roles": ["maf","clinical","metainfo"], "missing_roles": [], "message": "所需数据角色齐全，可以执行。" },
    "selection_summary": "胶质瘤队列 HRA000071 的 T2 处理后数据，MAF/临床/样本信息齐全，满足 TMB 生存分析。",
    "debug": { "intent": {}, "candidates": [], "file_details": [], "data_combination": {}, "llm_metadata": {} }
  },
  "llm_metadata": { "used": false, "status": "force_rule" },
  "answer": "面向用户的中文摘要"
}
```

顶层字段：`nl`（原始问题）、`intent`（结构化意图）、`capabilities_count`（=14）、`result`（完整结果）、`matched_pipelines`（按分排序的推荐）、`matched_data`（数据匹配）、`agent_input`（**下游执行接口直接可用的输入**）、`llm_metadata`（LLM 用量与状态）、`answer`（可读摘要）。

### 7.1 agent_input —— 面向执行接口的输入契约

下游执行接口需要的输入是「**匹配文件的绝对路径（每行一个）+ pipeline_id**」这种格式。系统在数据匹配完成后，自动为**首选 pipeline** 装配出该契约：

- `pipeline_id`：首选流程 ID（无匹配时为 `null`）；
- `files`：文件绝对路径数组，取自该流程的 `data_combination`（已按流程期望顺序排好，如 TMB 生存分析为 MAF → Clinical → MetaInfo），去重、保序；
- `files_text`：上面路径以换行拼成的文本块，可直接粘贴给执行接口；
- `feasibility`：**数据可行性检查**（见 7.2）——校验匹配到的文件是否覆盖该流程必需的全部数据角色，缺角色时 `ok=false` 并说明缺什么，让智能体先停下来要数据而不是拿残缺数据硬跑；
- `selection_summary`：**一句话数据选择说明**（见 7.3）——把 `debug` 里逐文件的规则打分日志（`match_reason`）翻译成给人看的一句话，讲清"为什么选这些文件"；
- `debug`：**供调试核对的推理信息**（不影响执行，仅用于人工检查为什么这样选）。

`debug` 复用了上游已经算好的信息，无需重跑路由即可回溯选择依据：

| 字段 | 含义 |
|------|------|
| `intent` | 解析出的结构化意图（分析目标、癌种、组学、格式线索等） |
| `candidates` | 每个候选流程的 `pipeline_id` / `name` / `confidence` / `reason` / `matched_terms`——即"为什么推荐它、命中了哪些词、置信几何" |
| `file_details` | 每个入选文件的 `path` / `input_role` / `format` / `source`（T1/T2/EXAMPLE）/ `study_accession` / `match_reason`——即"为什么选这个文件" |
| `data_combination` | 首选流程实际使用的数据组合（kind、所属 study、匹配原因） |
| `query_constraints` | 本次数据检索使用的约束（癌种、组学、格式、strategy、文件语义词） |
| `llm_metadata` | LLM 是否生效、模型名、token 用量与缓存命中（`used=false` 时为规则模式） |

示例（`agent_input.files_text` + `pipeline_id`）：

```text
/hpcdisk1/cbb_group/data/analysis/HRA000071/HRA000071-SomaticSNV-1.0.maf
/hpcdisk1/cbb_group/data/analysis/HRA000071/HRA000071-Clinical-1.0.xlsx
/hpcdisk1/cbb_group/data/analysis/HRA000071/HRA000071-MetaInfo-1.0.xlsx

tmb_survival_analysis
```

调试信息示例（节选自同一次请求的 `agent_input.debug`）：

```json
{
  "candidates": [
    {"pipeline_id": "tmb_survival_analysis", "confidence": 0.99,
     "reason": "命中 tmb, 肿瘤突变负荷, 生存分析，适合TMB 与生存分析。 LLM 闭集选择也命中。",
     "matched_terms": ["tmb", "肿瘤突变负荷", "TMB", "生存分析", "maf"]}
  ],
  "file_details": [
    {"path": ".../HRA000071-SomaticSNV-1.0.maf", "source": "T2", "format": "MAF",
     "match_reason": "癌种/队列匹配; 格式匹配 MAF; 优先使用处理后的 T2 数据; 文件语义匹配: SomaticSNV"}
  ],
  "data_combination": {"pipeline_id": "tmb_survival_analysis", "kind": "mutation_bundle", "study_accession": "HRA000071"},
  "llm_metadata": {"used": true, "model": "deepseek-v4-pro", "total_tokens": 2893}
}
```

### 7.2 feasibility —— 数据可行性 / 缺口检测

数据匹配阶段"尽力凑文件"，即便凑不齐也会返回候选。对执行接口最危险的正是"文件少一个还硬跑"——例如生存分析缺了临床数据，流程照跑却得到错误结论。`feasibility` 就是在把输入交给执行接口前做的一道**必需数据角色校验**：

- 取首选 pipeline 在 Data Profile 里登记的必需角色集合（如 TMB 生存分析必须有 `maf` + `clinical` + `metainfo`）；
- 与实际匹配到文件的角色（由文件名/格式推断）逐一比对；
- 缺任何角色 → `ok=false`，`missing_roles` 列出缺什么，`message` 给出人话提示；文件数少于流程要求（如配对样本 R1/R2 不齐）同样报不通过。

| 字段 | 含义 |
|------|------|
| `ok` | 数据是否齐全、可直接执行 |
| `required_roles` / `present_roles` / `missing_roles` | 需要的、已具备的、还缺的角色 |
| `required_file_count` / `actual_file_count` | 要求文件数 / 实际匹配数 |
| `message` | 一句话结论（缺什么、为什么不能跑） |

这一步**纯用已有的角色信息判断，不额外调 LLM**。当 `ok=false` 时，可读摘要 `answer` 里也会带上 `⚠️ 数据可行性检查` 提示，智能体据此可先向用户要数据，而不是产出错误结果。

### 7.3 selection_summary —— 数据选择的自然语言说明

`debug.file_details[*].match_reason` 是规则打分的原始日志（"癌种/队列匹配; 格式匹配 MAF; 优先使用处理后的 T2 数据…"），适合核对但不适合直接给人看。`selection_summary` 用 LLM 把这些逐文件打分依据 + 意图 + 可行性归纳成**一句话**，讲清"为什么是这批文件"：

> 胶质瘤队列 HRA000071 的 T2 处理后数据：体细胞突变 MAF、临床 XLSX 和样本信息 XLSX，癌种匹配且格式正确，满足 TMB 生存分析需求。

生产路径默认用 LLM 生成；`FORCE_RULE=1`、设置 `ROUTER_NL_SUMMARY=0`、或 LLM 不可用时，自动降级为确定性模板句（癌种 + 目标 + 角色清单 + 可行性），保证离线可用、绝不报错。

---

## 8. 效果演示

**输入**：
> "配对 WES 数据完成体细胞变异检测"

**系统识别**：`analysis_goal = 体细胞变异检测`，`omics_type = WES/MAF`

**推荐 Top1**：`wes_somatic_pair`（体细胞 WES 配对样本变异检测流程）

**内部流程**：`PreprocessTumor → PreprocessNormal → Mutect2Pair → PostprocessAndAnnotate → MultiQC`

**数据组合类型**：`paired_fastq`（自动配好的 R1/R2）

单次问答即完成「选流程 → 看步骤 → 配数据」的完整闭环。

---

## 9. Benchmark 结果

基于两份标准表（96 例问题-数据-工具对应表、问题与调用工具 150 例）评测。下表为 **DeepSeek（`deepseek-v4-pro`）实跑结果**：`FORCE_RULE=0`、逐题真实调用 API，96/96 与 150/150 均由 LLM 生效（无一降级到规则）。数据评分口径严格按「文件集合完全一致」，**多一个或少一个文件都判错**。

**96 例 benchmark（DeepSeek 实跑）：**

| 指标 | 结果 | 说明 |
|------|-----:|------|
| Pipeline Top1 Accuracy | **100%** (96/96) | 排第一的流程等于标准答案 |
| Pipeline Top3 Recall | **100%** (96/96) | 标准流程出现在前三 |
| Data 精确匹配（多一/少一均算错） | **100%** (96/96) | 预测文件集合 == 标准文件集合 |
| End-to-end（Top1 且数据精确） | **100%** (96/96) | 流程与数据同时全对 |

**150 例 benchmark（DeepSeek 实跑）：**

| 指标 | 结果 | 说明 |
|------|-----:|------|
| Pipeline Top1 Accuracy | **96.67%** (145/150) | |
| Pipeline Top3 Recall | 96.67% (145/150) | |
| 多工具集合精确匹配 | 95.33% (143/150) | 多工具题需集合完全一致 |

> **150 例的 5 道未命中中，有 3 道属于标准表行错位（第 138–140 题问的是"按 TMB 分组比较 PFS 生存"，模型正确判为 `tmb_survival_analysis`，但 gold 被错标为 `wgcna`）**；本表按原始标注如实计分、未替 gold 纠错。若剔除标注错位，真实准确率更高。另 2 道（单细胞下游做基因集富集 / 表达谱做 WGCNA）属边界语义。150 例表无标准数据文件列，故不含数据召回指标。

**关键改进——KEGG / GO 提示词强化**：在首轮 DeepSeek 实跑中，96 例曾有 7 道"差异表达 + 通路富集"题被误判为 `diff_expr_go`（GO 富集）而非 `diff_expr_kegg`（通路富集）。通过在 pipeline 路由 prompt 中加入**「GO 富集 vs 通路富集」严格区分规则**（凡"通路 / pathway / KEGG / Reactome / 信号通路"归 `diff_expr_kegg`；凡"GO / 功能富集 / 生物学过程"归 `diff_expr_go`；仅提其一不附带另一个，二者都提才都返回），这 7 题全部修正，96 例达到满分。

> 对比参考：纯规则模式（`FORCE_RULE=1`）此前基线为 96 例 Top1 88.54% / Data All-files 93.75%、150 例 Top1 97.14%。启用 DeepSeek 并强化提示词后，96 例三项指标均达 100%。

---

## 10. 关键设计决策

| 决策 | 理由 |
|------|------|
| **去 Neo4j，改用 CSV 导出** | KG CSV 镜像图实体；演示与评测无需启动图服务，降低部署门槛 |
| **LLM 为主、规则保底** | 规则保证可复现下限与离线可用；LLM 抬高语义理解上限；二者融合而非互斥 |
| **LLM 闭集约束** | 禁止自造流程名，机制性规避幻觉 |
| **LLM 只输出结构化字段** | 输出交确定性程序校验，LLM 不控制最终响应结构 |
| **全链路无异常穿透** | 每层 try/except 兜底 + 优雅降级，保证鲁棒性 |
| **零第三方 MCP 依赖** | JSON-RPC over stdio 手写实现，包体自包含、易分发 |
| **密钥外置** | API Key 严禁写入代码/配置/压缩包，仅经环境变量注入 |

---

## 11. 当前限制

- **尚未真正执行 pipeline**：只做推荐、解释、数据匹配，不跑 WDL/Cromwell/Docker/R 脚本；结果是"可执行前的结构化计划"，非"分析已完成"。
- **数据匹配以候选召回为主**：尚未验证文件真实存在性、schema 合规性、表达矩阵/MAF 与 clinical/metainfo 的样本 ID 可映射性、FASTQ 是否真为 tumor-normal 配对、样本量是否满足统计需求——这些属执行前校验与运行时 QC，待补。
- **Benchmark 存在闭集偏置**：评测与 14 个流程高度对应，主要反映闭集路由能力；真实开放问题可能超出闭集、需多流程串联、需补参数或需追问，后续需加 out-of-scope 检测与追问机制。

---

## 12. 下一步计划

1. **数据校验层**：文件存在性、格式/表头合规、样本 ID 跨表映射、R1/R2 配对、tumor-normal 同源性等；
2. **参数补全**：分组变量、目标基因（HER2/EGFR）、生存终点（OS/PFS）、差异阈值、WGCNA 目标性状、CellRanger expected cells、WES reference/interval 等；
3. **追问机制**：信息缺失时不强推，而是反问（癌种？分析目标？从 FASTQ 还是已有矩阵开始？GO 还是通路？）；
4. **从推荐走向执行**：自然语言 → 流程推荐 → 数据匹配 → 参数补全 → 执行前校验 → Cromwell 运行 → 结果索引入 KG → 报告生成，把系统从"Planner"推进为"可运行、可验证、可追踪"的生信分析 Agent。

---

## 13. 附：14 个流程分类一览

| 类别 | 流程 |
|------|------|
| 上游数据处理 | cellranger_workflow（单细胞）、rnaseq_singletask（RNA-seq 定量）、wes_somatic_pair（配对 WES 变异检测）、paired_fastq_to_unmapped_bam（FASTQ→uBAM） |
| 表达分析 | diff_expr_go、diff_expr_kegg（差异表达 + 富集）、rnaseq_unsupervised_cluster（无监督分型）、wgcna（共表达网络）、immune_infiltration_iobr（免疫浸润） |
| 突变分析 | wes_somatic_maf_landscape（突变景观）、driver_gene_gender_analysis（驱动基因性别分层） |
| 生存分析 | survival_analysis（基因突变状态）、tmb_survival_analysis（TMB）、her2_pfs_survival（HER2 表达） |

---

## 14. 一句话总结

**本系统在自然语言需求与实验室流程/数据资产之间构建自动化路由：LLM 只在 Neo4j 闭集内选择 pipeline 或 atomic tool，程序用 slot、NEXT 和 `HAS_STEP` 复核，再与 CSV 数据资产匹配，输出「流程 + 步骤 + 数据」的 `tool-chain/v1` 结构。运行参数由执行端负责，不影响编排状态。**
