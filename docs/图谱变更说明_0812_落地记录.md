# 0812 图谱落地记录

> 记录把 0812 知识图谱接进后端时哪些完全按数据提供方（师姐）的来、哪些按我方来，
> 以及每处偏离的原因。上一版记录见 git 历史里的 `图谱变更说明_0811_落地记录.md`。

## 0. 相对 0811 变了什么

0812 基本是照着 `docs/0811图谱修改建议清单.md` 改的，8 条建议修了 7 条：

| 上一轮提的 | 0812 |
|---|---|
| sample 表补样本级 `tissue_type`/`specimen_type` | 修了，9,700 / 9,279，另送 `biospecimen_anatomic_site` 8,676、`tumor_descriptor` 8,740 |
| fastqc（T002）输入输出标错 | 修了，输入改 R1/R2 配对，输出改 `QC_STATS_REPORT` |
| `METADATA_SAMPLEINFO` 拼写分裂 | 修了，统一到 `METADATA_SAMPLE_INFO`，13 个工具全改 |
| BAI 没有工具产出/消费 | 修了，T007 补输出、T008 补输入 |
| 缺语义格式父子关系 | 修了，新增 `reference/format_subclass.csv`，建成 `subclass_of` 边（我方给 3 行，她加了 4 行）|
| 删掉拼错的 `MUTATION_ANNOTATION_FORMA_MAF` | 删了 |
| `fastp→bwa` 的 kind 从 order 改 data | 改了 |
| `individual.csv` 整体错位一行（449 + 115）| **没修**，与 0811 逐字节相同 |

数据层几乎没动：T1、T2、study、project 与 0811 逐字节一致；individual 只补了
`population_type=cancer` 并删掉三个我方没用过的列（`sample_count`/`file_count`/
`organization`）；T2 删掉 `strategy_original`。

## 1. 落地结果：Neo4j 里只有她的图

```
节点 80,293   关系 352,252
标签 T2 36759 / T1 26089 / sample 10178 / individual 7050 / function 90
     tool 51 / format 28 / study 20 / project 18 / modal 6 / datalevel 4
```

与她自带 Neo4j 实例（`neo4j-community-2026.0812.zip` 内的 `data/databases/neo4j`）
**逐项一致**，基准存在 `config/senior_0812_reference_counts.json`。校验口径：

- 节点总数、关系总数、每个标签计数、每个关系类型计数，全部相等；
- 标签集合与关系类型集合完全相同，**没有任何我方新增的标签**；
- `sample` 节点的属性键集合与她的 `sample.csv` 列完全一致，**没有任何我方新增的属性**；
- `data/0812/` 下 31 个 CSV 与她原始 zip **逐字节一致**，不多不少。

`scripts/python/import_0812.py` 一步产出这个结果，没有任何后处理。远程服务器用
`import_0812_remote.py`（把 `LOAD CSV` 改写成 `UNWIND`），产出同源。

## 2. 我方的东西放在哪里

槽位名、WDL 绑定（`RNASeqPipeline.gtf_file` 这类）、输入变体、GATK tumor/normal 四槽——
这些是**执行端合同，不是知识图谱事实**，她的图里没有也不应该有。它们留在
`data/csv/catalog/`，由 `tool_catalog_source.py` 在运行时与她的图合并：

| 内容 | 来源 |
|---|---|
| 有哪些工具（51）、`has_function`、`suitable_for`、语义级 IO | 她的图 |
| NEXT 拓扑 | 她的 `next_tool` |
| 槽位名、`builder_param`、`wdl_target`、变体、GATK 四槽 | `data/csv/catalog/` |
| NEXT 数据边的 output/input 绑定 | `data/csv/relations/tool_relationship.csv` |
| `rnaseq_singletask` 的锁定步骤 | `data/csv/catalog/relationships.csv` 的 HAS_STEP |

两边不一致的地方不会被静默处理，而是进 `tool_catalog()` 返回值的 `divergence` 字段。

## 3. 完全按她的部分

- 全部实体、关系、属性、空值处理原样导入。
- 标签小写风格、`T1`/`T2` 大写、关系类型小写全部保留。
- 已知缺口按她的决策保留，不做任何填补：individual 7209 行经 MERGE 去重为 7050、
  T1 有 6848 行缺 `sample_accession`、T2 有 7 行 `semantic_format` 为空。
- 没有 `run` 和 `cohort` 节点，未补建。
- NEXT 拓扑与她逐条相同。

## 4. 按我方来的偏离项（共 4 处，比 0811 少 1 处）

### 4.1 tumor/normal 角色：以她的 `tissue_type` 为准，两个 study 覆盖

0812 的 `tissue_type` 给出 9,143 个样本的 Tumor/Normal，比上一版旁路补丁的 8,353 还多，
**旁路补丁与其生成脚本已删除**。但她填错了三处，用样本名里的 T/N 标记做仲裁能坐实
（全库 3,048 个带明确标记的样本里只有一处矛盾）：

| study | 她的值 | 证据 | 处理 |
|---|---|---|---|
| HRA016026 肺癌 700 例 | 整个 study 全标 Normal | 其中 350 个名字就叫 `L0240_Tumor` | `STUDY_ROLE_OVERRIDES` 按名字后缀判 |
| HRA000071 胶质瘤 | 286 个 `B_` 血样里 104 个标 Tumor、182 个标 Normal | 同前缀同 specimen 却分两派，血样在此是对照 | 按 `specimen_type` 统一判：Blood→normal |
| HRA000001 557 例 | `tissue_type='Blood'`、`specimen_type='Natural'` | 材料填进了角色列，老错位 | 不是 Tumor/Normal，自然判不出，不处理 |

反过来，我方上一版的 `HRA001748`/`HRA001749` 两条 `study_constant` 规则是错的——
把整个 study 一刀切成 tumor / normal，而她和更早的库都显示这两个 study 是混合的，
这两条规则已删除。可配对 study 从 8 个（实际能用的更少）变成 **11 个**。

### 4.2 multiqc 排除在原子层之外

她的图里 multiqc（T013）有 12 条入边。但 multiqc 是终端 QC 汇总器，没有下游消费它的
输出，把它放进原子链只会产生过不了闭集校验的 fan-in。运行时目录排除 multiqc
（50 个工具而不是 51）。

### 4.3 `read_pair` 从语义格式推导

她的 T1 没有 `read_pair` 列。优先用 `semantic_format`（`RAW_PAIRED_END_R1_FASTQ` /
`_R2_`），覆盖率 100%；文件名正则作为兜底。纯代码改动，不碰图。

### 4.4 `strategy` 归一到路由词表

她的 T1 保留原始策略 `WGS`/`bulk_RNA`/`WXS`/`WES`/`Targeted-Capture`/`TCR-Seq`/
`Unknow`/`sc-RNA`，而路由 profile 匹配的是 `WES`/`WGS`/`RNA-Seq`/`scRNA-seq`。
按她自己的 `reference/strategy_modal_map.csv` 折叠后再映射。484 行 `Unknow` 她标了
`action=skip`，保留原值不猜测。纯代码改动，不碰图。

> 0811 的第 5 处偏离（`fastp→bwa` 我方按 data、她标 order）已经消失：她这版改成 data 了。

## 5. `subclass_of` 对我方运行时没有影响

她新增的 4 条 format 父子边解决的是**图上按语义格式找工具**的问题：数据侧标
`DNA_ALIGNMENT_BQSR_BAM`，工具侧写 `DNA_GENOMIC_ALIGNMENT_BAM`，词表里两者平级。
在图里这确实让工具能认的文件从 83.7% 提到 94.0%。

但我方运行时**从不比对语义格式**：`pipeline_router._role_of_file()` 是子串启发式
（`"bam" in fmt`），`DNA_ALIGNMENT_BQSR_BAM` 一直就能识别成 BAM，从 BAM 出发的需求
（CNV、体细胞变异）本来就是通的。工具链拼接走的是槽位名与 `data_edges`，也不看语义格式。
所以这条改动照单导入进图，运行时不受影响，价值在于给直接查图的人用。

## 6. 她的模型表达不了、必须由我方补的

| # | 内容 | 说明 |
|---|---|---|
| 1 | GATK tumor/normal 四槽 | 她的 gatk 只有一个 BAM 输入，虽然这版补了 BAI，仍无 tumor/normal 之分 |
| 2 | 输入变体（单端/双端、单样本/配对） | 她的模型没有变体概念 |
| 3 | `rnaseq_singletask` 的锁定步骤分解 | 她的 T059 是扁平工具 |
| 4 | 槽位必填/选填、`one_of_group` | 她的模型没有这些标志 |
| 5 | 槽位名与 WDL 绑定 | 执行端 Knowledge Card 合同键，属执行端知识 |

这些全部留在 `data/csv/catalog/`，不进图。

## 7. 验证

| 项目 | 结果 |
|---|---|
| `data/0812` 31 个 CSV 与原始 zip 比对 | 逐字节一致，无缺失无多余 |
| 导入后与她的实例比对 | 节点、关系、标签集合、关系类型集合全部相等 |
| `sample` 属性键与她的 CSV 列比对 | 完全一致 |
| 全量单测 | 95 通过 / 4 跳过 |
| `health_check`（本机与服务器） | `ready=true`，tool_count 51/51 |
| 30 条自拟提问探针 | **30/30** |
| 角色仲裁：样本名 T/N 标记 vs 她的 `tissue_type` | 3,048 个带标记样本，只有 HRA016026 的 350 个矛盾 |

## 8. 已知缺口

1. `individual.csv` 的整体错位一行（449 个个体 + 115 个 sample 的 study 归属），
   这版没修，见 `docs/0812图谱修改建议清单.md`。
2. 她填的 tumor/normal 有三处错，当前靠 `STUDY_ROLE_OVERRIDES` 兜底，她修好后可以删。
3. **配对 WES 稳定性约 5/6**。残余失败是单次 LLM 调用偶尔返回空，不是校验或数据问题。
4. 按 study 反查"这批数据能做什么"未实现，能力问答是目录级的。
5. `docs/mcp_delivery/` 下的交付快照仍是更早的版本，未随本次更新重新打包。

## 9. 复现步骤

```bash
export NEO4J_PW='...'
python3 scripts/python/import_0812.py --project-root . \
  --neo4j-import-dir ~/Downloads/neo4j-community-2026.06.0/import \
  --uri "$NEO4J_URI" --user "$NEO4J_USER" --password-env NEO4J_PW \
  --database "$NEO4J_DATABASE" --expected-database-id <db-id> --confirm-clear

python3 scripts/python/validate_csv.py --project-root .
python3 -m unittest discover -s tests
python3 scripts/probe_30_prompts.py
```

导入后不需要任何目录同步步骤——工具目录不进图。
